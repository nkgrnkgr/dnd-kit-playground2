import { Box, VStack } from "@chakra-ui/react";

export const Form = () => {
  return (
    <Box
      sx={{
        bgColor: "gray.100",
        p: "12px",
        h: "100vh",
        minWidth: "800px",
      }}
    >
      <VStack spacing="12px"></VStack>
    </Box>
  );
};
